<?xml version="1.0" encoding="UTF-8"?> <!-- 项目名称test,可用全局替换为当前项目名称 -->
<project>
    <property name="defaultPackageName" value="com.caipiao.sanliuwu" />
    <property name="defaultPackagePath" value="com/caipiao/sanliuwu" />
    <property name="newPackageName" value="com.bbl.gmchy" />
    <property name="newPackagePath" value="com/bbl/gmchy" />
    <!--<echo>current directory: ${basedir}</echo>-->

    <target name="build">
        <!-- Replaces all of the references to the old package name in files in the "src" directory -->
        <replace dir="app/src" summary="true" token="${defaultPackageName}" value="${newPackageName}" />

        <!-- replaces the package name in the manifest -->
        <replace file="app/src/main/AndroidManifest.xml" summary="true" token="${defaultPackageName}"
            value="${newPackageName}" />

        <!-- renames the src folders -->
        <move toDir="app/src/main/java/${newPackagePath}">
            <fileset dir="app/src/main/java/${defaultPackagePath}" />
        </move>
    </target>
</project>